// Stub for connector text blocks/deltas.
// Upstream includes connector block typing for certain client integrations.
// The leaked `src/` dump references this module but it is absent.

export function isConnectorTextBlock(_x) {
  return false
}

