// Stub for generated SDK types.
//
// The upstream project generates this file from Zod schemas (see coreSchemas.ts).
// This leaked `src/` dump does not include the generated artifact, but many
// modules re-export it. For baseline testing we only need module resolution to
// succeed; the type surface is validated via runtime schemas elsewhere.
export {}

